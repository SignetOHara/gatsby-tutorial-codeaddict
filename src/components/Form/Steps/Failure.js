import React from "react";

// import classes from "../Form.module.scss";

// Need to make sure styling is red and correct mobile number
const Failure = () => {
  return (
    <section >
      <div >
        <p>The form was unable to send. Please use the facebook Live chat to enroll directly or call: xxxxxx</p>
      </div>
    </section>
  );
};

export default Failure;
